<div class="container">
  <div class="row">
    <% myDogs.forEach((dog, index) => { %>
    <div class="col s12 m6">
      <div class="card">
        <div class="card-image">
          <img src="<%= dog.image %>" />
        </div>
        <div class="card-content">
          <h5 class="card-title"><%= dog.name %></h5>
          <p><%= dog.name %> is famous for <%= dog.famousFor %></p>
          <br />
          <a href="/dogs/edit/<%= index %>"
            ><input type="submit" value="Edit"
          /></a>
          <br /><br />
          <form method="POST" action="/dogs/<%= index %>/?_method=DELETE">
            <input type="submit" value="Delete" />
            <br />
          </form>
        </div>
      </div>
    </div>
    <% }) %>
  </div>
  <div class="center-align">
    <a class="waves-effect waves-light btn-small right-align" href="/dogs/new"
      ><i class="material-icons right">add</i>Add Dog</a
    >
    <a class="waves-effect waves-light btn-small" href="/"
      ><i class="material-icons right">send</i>Back to home</a
    >
  </div>
</div>

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The famous dog pack</title>
  <link rel="stylesheet" href="/styleDogs.css">
</head>

<body>

  <form method="GET" action="/dogs">
    <label for="nameFilter">Filter by name</label>
    <input type="text" id="nameFilter" name="nameFilter">
    <input type="submit">
  </form>

  <ul>
    <% dogList.forEach((dog, index) => { %>
    <li>
      <%= dog.name %> is famous for <%= dog.famousFor %>
      <a href="/dogs/edit/<%= index %>">Edit/Delete</a>
      
    </li>
    <div>
      <img class="indexImageDogs" src="<%= dog.image %>" alt="a picture of <%= dog.name %> ">
    </div>
    <form action="/dogs/<%= index %>">
      <input type="submit" value="See the big picture">
    </form>
    <% }) %>
  </ul>

  <h2><a href="/dogs/new">Add a new dog to the list of Famous Doggos</a></h2>


</body>

</html>